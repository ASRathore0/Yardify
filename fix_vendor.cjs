const fs=require('fs');let c=fs.readFileSync('app/Http/Controllers/VendorController.php','utf8'); c=c.replaceAll('image' => 'nullable|image|max:4096',\r\n        ]);\r\n        if ( + '->hasFile(\'image\')) {\r\n             = ->file(\'image\')->store(\'vendors\', \'public\');\r\n            [\'image_path\'] = ;\r\n        }\r\n        unset([\'image\']);\r\n        ->update();', 'images' => 'nullable|array',\r\n            'images.*' => 'image|max:4096',\r\n        ]);\r\n        if ( + '->hasFile(\'images\')) {\r\n             = [];\r\n            foreach ( + '->file(\'images\') as ) {\r\n                [] = ->store(\'vendors\', \'public\');\r\n            }\r\n            [\'image_path\'] = json_encode();\r\n        }\r\n        unset([\'images\'], [\'image\']);\r\n        ->update();');fs.writeFileSync('app/Http/Controllers/VendorController.php',c);
